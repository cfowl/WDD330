

//----- NASA View -----//
export default class NasaView {
    buildFavoritesList(display) {
        // is this function necessary or could buildResultsList() be used??
    }

    buildResultsList(element, items) {
        // do stuff
        // this function will build the innerHTML for the result element
    }
    
    buildImageDetailsDisplay(display, item) {
        // do stuff
        // this function builds the innerHTML for the image details display
    }
    
    buildVideoDetailsDisplay(display, item) {
        // do stuff
        // this function builds the innerHTML for the video details display
    }
    
    buildBackButton(container) {
        // do stuff
        // this function builds the back button for the details display
    }
}